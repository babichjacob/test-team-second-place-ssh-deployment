name: Deploy

on:
  push:
    tags:
      - "*"

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: "x86_64-unknown-linux-musl"
      - uses: Swatinem/rust-cache@v2
      - uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_KEY }}
          known_hosts: ${{ secrets.KNOWN_HOSTS }}
      - run: cargo build --release --target x86_64-unknown-linux-musl
      - run: rsync --verbose ./target/x86_64-unknown-linux-musl/release/test-rust-ssh-deployment ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/opc/
      - run: rsync --verbose ./tsp-rust-ssh-deployment.service ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/home/opc
      - run: cat ./install-service.bash | ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}
